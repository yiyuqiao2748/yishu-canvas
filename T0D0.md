# T0D0

更新时间：2026-07-25

## 当前已推进

- 智能画布云端模式：已支持轮询云端版本、保存中排队、失败重试、冲突提示和自动合并后重存。
- 普通画布云端模式：已支持自动保存失败重试、云端冲突提示，并在冲突时合并远端新增节点和连线后继续保存。
- 本地数据读写入口：已完成阶段 0 审计，详见 `docs/migration/local-data-readwrite-audit.md`。
- 团队素材存储：新增 `TEAM_ASSET_REQUIRE_R2`，线上可禁止未配置 R2 时静默写入本地盘。
- GitHub 推送：当前使用 UTF-8 GitHub API 推送，避免本机 Git 登录状态影响。

## 下一步优先级

- 线上验收：团队成员登录、创建团队、创建项目、创建云端画布、上传团队素材。
- 画布验收：分别测试智能画布和普通画布在两个浏览器窗口同时编辑时的保存、提示和合并表现。
- 存储验收：确认团队素材当前落到 NAS 本地目录，还是切换到 Cloudflare R2。
- 阶段 3 代码改造：开始抽象 AI 生成结果的统一文件存储入口，为后续写入 Cloudflare R2 做准备。
- 线上配置：等能操作 NAS 后，将 R2 配齐并把 `TEAM_ASSET_REQUIRE_R2=true`。

## 暂时受阻

- 公司电脑当前不能操作飞牛 NAS，所以 NAS `.env`、R2 环境变量和线上容器重启暂时不做。
