# T0D0

## 2026-08-10 Current Project Progress

| Area | Status | Notes |
| --- | --- | --- |
| NAS/Mac development setup | Done | New Mac can sync source to `yishu-nas:/vol1/1000/docker/yishu-canvas/yishu-canvas-fnos/`; deployment uses stop -> rm -> `build --no-cache` -> `up -d --force-recreate` so static UI changes take effect reliably. |
| Runtime persistence guard | Done | NAS sync/build excludes `.env`, `.env.*`, `data/`, `output/`, `assets/`, `team-assets/`, `api-env/`, Python caches, local tmp files, and logs so source deployment does not wipe runtime assets or secrets. |
| Admin usage MVP backend | Done | Added bootstrap/admin/usage/points/heartbeat foundations: usage logs, point ledger/balances, admin-only API surface, online heartbeat/session estimation, slow request logging, and config/catalog caching without adding Redis/ClickHouse. |
| Admin preview page | Done | Added isolated admin preview page with real admin API reads; entry is now available from homepage after admin permission checks, but the page remains a preview/admin-only workflow instead of a public user flow. |
| Login architecture shift | Done | Product now uses app-native account/password login with email-verification signup flow instead of relying on Cloudflare Access as the ordinary user login gate. Login remains username/email + password; signup is username + email + password, then email code verification. |
| Password recovery entry | Done | Workbench login modal now shows `?????`; recovery remains tied to backend reset/recover endpoints and reset password page. |
| Cloudflare Access removal for public app | Done | Removed the Access application that protected `canvas.yiyuqiaoai.uk`; Cloudflare Tunnel/DNS/R2 remain in place, and public checks return the app directly with `HTTP 200` instead of an Access login page. |
| Homepage UI/navigation polish | Done | Reduced duplicate top-nav actions, made points a non-clickable account balance status, shortened `????` to `??` and `AI??` to `??`, reduced the red-heavy background, and tightened the first-screen composer. |
| Auth modal UX polish | Done | Login mode only shows email/account + password + forgot-password; signup mode shows username + email + password; verification/resend controls stay hidden until the signup verification step; active tab contrast was strengthened. |
| Light/dark theme unification | Done | Workbench, canvas, asset manager, API settings, ComfyUI settings, canvas list, and agent panel have been swept for light/dark contrast problems and old dark-only surfaces. |
| Asset manager deletion UX | Done | Asset manager tabs and grouped canvas assets gained clearer delete/management affordances so selected assets and group items are no longer download-only. |
| Canvas asset persistence fix | Done | Missing-file issues after NAS rebuild were traced to runtime asset persistence/sync safety; runtime asset directories are excluded from source sync and Docker build context so rebuilds no longer remove user assets. |
| Feedback collection | Done | Workbench feedback modal submits to backend storage and is ready to surface in admin management later. |
| Homepage version label | Done | Workbench now shows the live `/healthz` VERSION as small text above `????`; static cache was bumped so future homepage changes remain visible after deploy/rebuild. |

### 2026-08-10 Verification

- [x] Local regression: `.venv/bin/python -m unittest tests.test_team_cloud` passed 63 tests.
- [x] Static checks: `node --check static/js/workbench.js` and `git diff --check` passed.
- [x] NAS rebuild: Docker Compose stop/rm/no-cache build/recreate completed; `yishu-canvas` reported healthy and `cloudflared` restarted.
- [x] LAN readiness: `http://192.168.1.3:3000/healthz` reports auth, Supabase, team API secret, R2, and R2 public URL readiness true.
- [x] Public access: `https://canvas.yiyuqiaoai.uk/` returns `HTTP 200`, dynamic Cloudflare response, and the `AI Studio` app shell without Cloudflare Access blocking.
- [x] Auth modal browser smoke: public page login mode shows only `?? / ??` and `??`, forgot-password is visible, resend-code is hidden; signup mode shows `???`, `??`, `??`, and selected tab contrast is readable.

### 2026-08-10 Follow-Up Plan

- [ ] Supabase Auth production tuning: confirm email OTP/confirmation template shows a Chinese 6-digit code, production URL is `https://canvas.yiyuqiaoai.uk`, and SMTP fallback is documented if default delivery is weak.
- [ ] Signup acceptance: create a fresh test account from the public site, verify email code, auto-login, default team, initial points, and `/api/team-cloud/me` consistency.
- [ ] Password recovery acceptance: request recovery from login modal, confirm the email flow reaches `/static/reset-password.html`, and verify old password invalidation after reset.
- [ ] Admin preview acceptance: review overview metrics, user list/detail, usage log filters, point adjustment ledger, and non-admin 403 behavior before treating it as the official admin page.
- [ ] Feedback admin view: add feedback list/detail to the admin preview so product feedback submitted from the homepage can be reviewed without opening server files.
- [ ] Usage billing refinement: after MVP fixed point costs are accepted, add per-model cost presets and editable model pricing in admin settings.
- [ ] Online duration refinement: improve heartbeat aggregation into daily/monthly active time summaries and expose current online/offline state in admin user details.
- [ ] Asset lifecycle management: add batch delete confirmations, recover/trash policy, and orphaned R2 object audit to avoid accidental loss and storage growth.
- [ ] Production UI audit: continue checking low-frequency pages after each theme change, especially light mode, mobile width, native select menus, and modal contrast.
- [ ] Git workflow: keep runtime secrets/assets out of GitHub; on another computer, clone/pull GitHub source, set local/NAS env separately, then deploy with the NAS sync/rebuild flow.

## 2026-07-27 Current Project Progress

| Area | Status | Commit | Notes |
| --- | --- | --- | --- |
| Supabase visibility constraints | Done | `75c8949` | Backend/schema-level `private` / `team` visibility constraint is in place. |
| Canvas list private/team grouping | Done | `479b4da` | `My Private` / `Team Shared` is now a real grouped filter with publish action and regression coverage. |
| Workbench mobile composer layout | Done | `edcb495` | Mobile composer controls no longer overflow when UI scaling is disabled. |
| Workbench composer creates seeded canvas | Done | `01e02ad` | Prompt composer reuses the existing canvas creation flow and seeds a prompt node. |
| Workbench composer seeds generator node | Done | `a8e6a5d` | Seeded canvas now also reuses the existing generator node and connects prompt -> generator without auto-running generation. |
| Workbench reference UI preview | Done | local preview | Added `static/workbench-preview.html` and `static/css/workbench-preview.css` as a high-fidelity preview of the supplied AI designer home screenshot without replacing the current homepage; browser comments applied: cartoon character hero asset, fixed user chip icon, `????` CTA copy, and left rail `????` entry that opens `canvas-list.html?cloud=1&visibility=private`. |
| Workbench homepage replacement | Done | local change | Promoted the approved high-fidelity preview structure/style into `static/workbench.html` and `static/css/workbench.css`; `static/index.html` now cache-busts the official workbench entry at `2026.07.28.3`. |
| Workbench homepage browser acceptance | Done | local Playwright | Desktop and mobile Chromium screenshots verified the promoted official homepage renders, keeps prompt/generate controls, uses the character hero asset, and has no horizontal page overflow or console errors. |
| Workbench browser comments pass | Done | local Playwright | Addressed browser annotations: account chip now loads `/api/team-cloud/me` and falls back to `???`, team members see `???` as `???` while logged-out users stay `0`, hero uses the user-owned `workbench-character.png`, prompt placeholder rotates with a typing effect, canvas/assets cards can use recent image thumbnails, and feedback submits to `/api/workbench/feedback` for backend JSONL storage. |
| Workbench full-home navigation pass | Done | local change | Homepage mode now hides the legacy outer sidebar and lets `workbench.html` own the full viewport; the left rail is a dark pill-style icon rail with hover labels, low-frequency local tools are collapsed behind a secondary reveal button, and the top dock keeps account, inspiration points, team, assets, workflow, API settings, and a black/white theme toggle. |
| Canvas editor visual alignment pass | Done | local verification | Canvas list, classic canvas, and smart canvas share the homepage dark red/gold grid, floating docks, compact tool controls, dark panels, and red/orange primary actions without changing canvas engine behavior. |
| Canvas immersive shell cleanup | Done | local verification | Canvas mode hides the legacy white outer sidebar like the homepage; classic canvas keeps core creation tools on the left rail and moves low-frequency tools/history/workflow/assets/logs into a top-right capsule action group. |
| Secondary page visual unification | Done | local browser verification | Text-to-image, enhance, edit, angle, online generation, GPT chat, team, asset manager, API settings, ComfyUI settings, and canvas list pages now use the same dark red/black glass workbench skin while preserving existing controls and API hooks. |
| Secondary page browser annotation polish | Done | local browser verification | Fixed the annotated UI issues: workbench history opens the canvas list instead of the team page, standalone theme toggle applies light/dark locally, canvas list has a home button, classic canvas low-frequency tools collapse by default, LLM/API prompt blocks use dark surfaces, and team/API/enhance/angle/Klein/GPT controls no longer fall back to white panels. |
| UI review stabilization pass | Done | local tests + Edge audit | Fixed stale static cache versions, iframe route param forwarding, shared theme application, API recommendation button contrast, angle/enhance control labels, canvas/team control names, and optional WebSocket noise; added a stale-version regression test. |
| UI review annotation follow-up | Done | local tests + Edge audit | Fixed immersive API/assets iframe layout so the legacy sidebar no longer shows, added asset manager home navigation, changed workbench upload/reference URL controls into real pickers/modals, made prompt optimization update the composer in place, wired generation to `/api/online-image`, and changed history to open recent canvases. |
| Team management immersive shell | Done | local Edge audit | Team Cloud login/management now joins the same immersive iframe mode as workbench/canvas/API/assets, so the legacy outer white sidebar no longer remains visible around the team page. |
| Workbench theme and admin gate follow-up | Done | local tests | Shared theme application now writes both studio/workbench classes so child pages can follow light/dark state; canvas list gained a light workbench skin, the Team Cloud page no longer exposes its own Team API panel, and API settings are gated behind an authenticated owner/admin entry from the homepage account/admin modal. |
| Full child-page contrast audit | Done | local + LAN Chrome render scan | Checked the studio shell plus all 12 iframe child pages; fixed native select option menus, API settings field wrappers/recommend API panel, Team Cloud auth tabs, and Team API sidebar surfaces so local and NAS scans report no shallow-background/light-text risks. |
| Studio UI structural audit | Done | LAN Chrome render scan + NAS logs | Checked all 12 entry switches for duplicate active frames, old shell/sidebar overlap, duplicate IDs, contrast risks, and select menu risks. No mixed old/new UI rendering was found; the only real runtime issue was `/ws/stats` failing because the container lacked a Uvicorn WebSocket runtime dependency. |
| Smart Canvas AI Agent integration plan | Done | local tests + Edge smoke + NAS API smoke | Agent phase 1 is complete: authenticated Agent API, server-side JSON plan normalization, frontend Agent panel routed through `/api/canvas-agent/suggest`, prompt-node execution writes `node.text`, normalized `prompt/image/loop` cards execute, selected prompt optimization works, parameter suggestions apply only confirmed fields, authenticated memory/level hydration is wired, workflow plan cards and feedback sync status are visible, and Agnes video fallback passed real `/api/canvas-video` smoke. |
| AI Agent explicit model routing | Done | local tests | Agent no longer defaults to `comfly` or unverified `runninghub / gpt-4o-mini`; chat routes to `modelscope / Qwen/Qwen3-235B-A22B` when available, falls back to `agnes-ai / agnes-2.5-flash`, image plans point to `custom-api / nano-banana-2`, frontend sends the last 10 messages, plain-text LLM replies are preserved, and exact repeated assistant replies trigger one retry. |
| AI Agent chat credential guard | Done | local tests + NAS API smoke | Agent only uses Agnes chat fallback when the fallback platform has an API key; after the Agnes key was configured in the persistent NAS API env, authenticated `/api/canvas-agent/suggest` returned `agnes-ai / agnes-2.5-flash` for text-only chat and `custom-api / nano-banana-2` for explicit image planning. |
| Smart Canvas Agent UI polish | Done | local tests + Edge audit | Smart Canvas now has a unified red/black glass top-right dock with a homepage return action; the AI Agent panel opens as a centered floating bottom composer instead of a full-width bottom slab, and top-right submenu panels plus nested popovers were swept for old blue-gray panel residue. |
| Classic Canvas Agent/theme alignment | Done | local tests + Edge audit | Ordinary canvas now loads the shared Agent panel and uses a thin bridge over existing public node creation functions only, without changing the existing node engine. Classic canvas board, top dock, quick toolbar, minimap, LLM/API generator fields, workflow/assets/log panels, and modal popovers were swept to match the Smart Canvas red/black glass skin instead of the old blue-gray surface. |
| Workbench custom parameter selects | Done | local tests + Chrome stacking smoke | Replaced the homepage composer's visible native select popups with custom rounded glass menus while keeping the original select values as the data source; clicking the chevron area opens the menu, selected values sync back to the existing generation flow, CSS/JS cache versions were bumped, and the size/resolution/model menus now stack above the asset library instead of being covered. |
| UI polish NAS sync | Done | NAS share `\\100.66.1.1\docker\yishu-canvas\yishu-canvas-fnos` | Synced the changed source/static/docs/test files to NAS and verified key file SHA-256 hashes match local. `.env`, `data/`, and `output/` were not copied. Docker rebuild/restart remains the runtime acceptance gate. |
| Workbench generate button binding | Done | local browser verification | Fixed the promoted homepage so every `data-workbench-generate` button binds to the generation flow; the primary composer button now triggers the empty-prompt guard and will seed canvas creation when a prompt is present. |
| Workbench browser smoke | Done | local verification | Headless Chrome verified workbench composer -> canvas-list -> canvas creates visible prompt node, generator node, and prompt-to-generator connection. |
| Workflow Builder v1 | Done | local tests + JS checks | Classic and Smart Canvas now share fixed built-in workflow templates for text-to-image, image-to-image, and image-to-video. Dragging or opening a template only creates nodes and links; image workflows can create an empty image input node when no image is selected; Nano Banana is the default image plan and Agnes video remains an optional fallback node. |
| Canvas list cloud delete action | Done | local tests + JS checks | Cloud canvas cards now show Delete in the three-dot More menu and call the existing Team Cloud DELETE endpoint; local canvases still use the local soft-delete/trash flow. |
| grsai Nano Banana 4K async generation | Done | `18bf722` + NAS acceptance | `custom-api / grsai` routes both `nano-banana-2` and `nano-banana-pro` through the documented `/v1/api/generate` endpoint. For `2K/4K`, keep `imageSize` as `2K/4K`, send `replyType=async`, accept grsai numeric task ids such as `6-...`, then poll `/v1/api/result?id=...` for `results[].url`. Do not revert this to the generic OpenAI image path or to task ids that only start with `task`. |
| Classic/smart canvas kind acceptance | Done | local verification | Headless Chrome verified classic and smart cards keep distinct kind badges and route to `canvas.html` / `smart-canvas.html` correctly. |
| Local asset drag-to-canvas smoke | Done | local verification | Headless Chrome verified `/api/local-assets/upload`, local asset listing, `application/x-canvas-asset` drop, visible image node, and saved canvas JSON. |
| NAS file sync | Done | NAS copy verification | Copied changed files through `a672039` to `Z:\yishu-canvas\yishu-canvas-fnos`; hash check matched all copied files. |
| NAS Docker rebuild/restart | Done | SSH verification | Ran `docker compose -p yishu-canvas up -d --build`; `yishu-canvas` restarted healthy and LAN static JS includes the latest workbench seed-generator code. |
| NAS LAN healthz readiness | Done | `http://192.168.1.3:3000/healthz` | LAN healthz reports auth, Supabase, R2, R2 public URL, and team API secret readiness true; `dev_bypass` false. |
| NAS API Key persistence | Done | local test + NAS mount verification | Root cause found: browser-saved provider keys are written to `/app/API/.env`, while only `/app/data` was persisted. `deploy/fnos/docker-compose.yml` now mounts `./api-env:/app/API`, and runtime paths are ignored so routine source sync does not overwrite keys. |
| NAS sync artifact guard | Done | local test + NAS cleanup | `deploy/sync-nas-source.ps1` and `.dockerignore` now exclude local-only `.venv`, `tmp`, and `.codex-speedtest-current`; the accidentally synced copies were removed from the NAS deploy directory before rebuild. |
| Classic canvas secondary tools dock | Done | local tests + NAS acceptance | The expanded top-right More tools dock now wraps instead of clipping, and the toggle visibly changes from `??` to `??`; NAS rebuild is healthy and LAN static CSS/JS include the fix. |
| Canvas node upload JSON error guard | Deployed | `7beeb9c` + NAS rebuild | Classic Canvas and Smart Canvas now route `/api/ai/upload` through guarded upload helpers: oversized files are rejected before upload, non-JSON/HTML responses produce a Chinese upload error instead of `Unexpected token '<'`, and local/LAN direct 9MB plus local real 3840x2160 upload probes return JSON. GitHub push, NAS sync, Docker rebuild, LAN `/healthz`, and running static JS guard checks passed; public browser retry with the user's 4K image remains the final user acceptance step. |
| Supabase schema applied | Done | SSH + `psql` verification | Ran `team_cloud_schema.sql` against production Postgres; verified `canvases.visibility` and `assets.visibility` are non-null default `team` with `private/team` check constraints. |
| Production visibility DB smoke | Done | rollback transaction | Inserted temporary `private/team` canvases/assets successfully, verified invalid `public` visibility is rejected, then rolled the transaction back. |
| Team-cloud API auth preflight | Done | real access token | `SUPABASE_JWT_SECRET` is empty, so Codex cannot mint a temporary JWT; production API smoke used a real Supabase access token instead. |
| Team-cloud production API smoke | Done | real Supabase access token | Created temporary team/project/private canvas, listed it, published it to `team`, uploaded/listed/read/deleted a 1x1 R2 team asset, then deleted the temporary team. |
| Team asset UI smoke | Done | headless Chrome + LAN production API | Uploaded a 1x1 team asset from the editor, dragged the team asset card onto the cloud canvas, saved the image node, then dragged an output image back into Team Assets and verified a second team asset was created. |
| Team API member permission smoke | Done | headless Chrome + Supabase service-role fixture | Created a temporary auth user as `member`, configured a temporary team provider, verified the member can read provider metadata, provider management returns 403, and the team API settings panel/admin controls are hidden in the UI. |
| Team API live provider readiness audit | Resolved | NAS env + production API | Initial blocker was provider id mismatch: the grsai key existed under team provider `openai`, while the global generator uses `custom-api`; the grsai config is now mapped to `custom-api`. |
| Team API live provider generation smoke | Done | Supabase service-role fixture + production generation API | Mapped the existing grsai team provider config to the global `custom-api` provider id, then a temporary `member` account generated with `custom-api` / `gpt-image-2`; production generation log shows `succeeded` with `image_count=1`. |
| Cloudflare Access config preflight | Done | NAS env + Supabase service-role read | Cloudflare Access config is present, default team exists, and default role is `member`; actual non-admin login still requires a real Cloudflare Access session/JWT from an allowed email. |
| Cloudflare Access non-admin login | Done | real Chrome Cloudflare Access session | Browser session at `https://canvas.yiyuqiaoai.uk` returned `/api/team-cloud/me` JSON with `provider=cloudflare-access`, email `yiyuqiao2748@gmail.com`, default team `8b79d1ce-5782-48ea-bead-a68b5cc56bfb`, and role `member`; note this differs from the typed `yiwei2748@gmail.com` address. |
| Cloud version API smoke | Done | real Supabase access token | Created a temporary cloud canvas, saved v2, verified stale `base_version=1` returns 409 with remote v2, listed versions `[2,1]`, restored v1 to v3, then deleted the temporary team. |
| Cloud version dual-browser UI smoke | Done | headless Chrome + LAN production API | Two independent Chrome profiles opened the same cloud canvas; browser B observed 409 then retried to 200, final version reached v3 with both A/B nodes. Editor version history listed v2/v1 and restored v1 to new v3. |
| Legacy admin login compatibility | Done | local tests | After email OTP signup was introduced, existing Supabase owner/admin accounts without `email_confirmed_at` can still log in with password and enter admin/API settings if they already have an owner/admin team membership. New or non-admin unverified accounts still require email verification. |
| Supabase dashboard user login compatibility | Done | local tests | Supabase Auth users created manually in the dashboard can log in with password once their email is confirmed; the backend now creates the missing `user_profiles` row on first confirmed login instead of returning the misleading email-code verification error. Admin/API access still requires an owner/admin `team_members` row. |

### Remaining Work

- [x] Production/NAS: apply `docs/supabase/team_cloud_schema.sql` before production verification.
- [x] Production/NAS: rebuild/restart Docker from `Z:\yishu-canvas\yishu-canvas-fnos\deploy\fnos` so the running container loads the synced code.
- [x] Production/NAS: confirm R2 and stable `TEAM_API_SECRET_KEY` readiness through LAN `/healthz`.
- [x] Production/NAS: verify actual online team API key behavior with a real provider call after a live team provider/key is configured.
- [x] Cloudflare Access acceptance: real browser session verified `cloudflare-access` user `yiyuqiao2748@gmail.com` is mapped into the default team as `member`; typed `yiwei2748@gmail.com` was not the returned account.
- [x] Canvas acceptance: verify classic canvas and smart canvas lists stay separated after open/create flows.
- [x] Local asset acceptance: verify local upload, local asset listing, drag-to-canvas, and saved image node path.
- [x] Team asset API acceptance: verify team asset upload/list/content/delete path against production R2 in a real team session.
- [x] Team asset UI acceptance: verify team asset drag-to-canvas and save-from-canvas-to-team-assets paths in a real browser session.
- [x] Team API acceptance: member API settings are hidden/403 verified; temporary member generated through the configured grsai `custom-api` provider with `gpt-image-2`.
- [x] Cloud version API acceptance: verify stale save conflict, version listing, and restore behavior through production API.
- [x] Cloud version UI acceptance: run dual-browser conflict/version-history restore verification.
- [x] Workbench browser smoke: from prompt composer, create a canvas and confirm prompt node + generator node + connection are visible before generation.
- [x] Workbench reference UI preview: create a high-fidelity preview page from the supplied screenshot without replacing the current homepage.
- [x] Workbench homepage replacement: after visual approval, switch the production homepage from `workbench.html` to the approved preview UI.
- [x] Workbench homepage screenshot acceptance: verify the promoted official homepage in a real browser on this machine.
- [x] Workbench browser comments: apply account, inspiration points, user character image, prompt typing placeholder, thumbnail card hooks, and backend feedback storage.
- [x] Workbench full-home navigation: move the legacy sidebar entries into the homepage rail, add top-right API/theme controls, and make the homepage cover the full studio shell.
- [x] Canvas editor visual alignment: first-pass classic/smart canvas visual skin now matches the homepage workbench direction without changing canvas engine behavior.
- [x] Canvas immersive shell cleanup: canvas mode hides the old outer sidebar and moves low-frequency classic canvas tools to a top-right capsule group.
- [x] Secondary page visual unification: generation pages, GPT chat, team, asset/API/workflow settings, and canvas list share the homepage skin.
- [x] Browser annotation polish: fixed remaining white panels, canvas toolbar overflow, canvas-list home navigation, history shortcut routing, and standalone theme toggle behavior.
- [x] UI review stabilization: restored current static cache versions, preserved workbench iframe params, unified theme fallback/application, removed standalone horizontal overflow, and verified API/angle/enhance/GPT route polish in Edge.
- [x] UI review annotation follow-up: removed old shell residue on API/assets iframe pages, added asset-manager home navigation, wired workbench upload URL/reference controls, in-place prompt optimization, direct online image generation, and recent-canvas history routing.
- [x] Team management immersive shell: team login/management now opens full-screen inside the outer homepage shell with the legacy white sidebar hidden.
- [x] Workbench theme/admin gate follow-up: child pages receive shared `studio_theme` / `canvas_theme` classes, canvas list follows the light homepage skin, Team Cloud no longer shows a Team API panel, and API settings require an authenticated owner/admin entry.
- [x] Full child-page contrast audit: local and NAS Chrome loaded the studio shell and all 12 iframe child pages; native select menus, API settings recommendation panel, and Team/API settings surfaces no longer report shallow-background/light-text risks.
- [x] Studio UI structural audit: LAN Chrome switched all 12 entries and found no duplicate active iframe, no old shell/sidebar overlap in immersive pages, no duplicate IDs, and no contrast/select risks; `/ws/stats` console errors traced to a missing WebSocket runtime dependency.
- [x] Smart Canvas Agent UI polish: top-right Smart Canvas dock now has unified glass color and homepage return, and the Agent panel floats centered near the bottom instead of spanning the full bottom.
- [x] Classic Canvas Agent/theme alignment: ordinary canvas now shares the Agent panel and final red/black glass skin through a thin adapter that calls existing node creation APIs only; existing node state and execution logic were not rewritten.
- [x] Workbench custom parameter selects: homepage size/resolution/model controls now use custom rounded glass menus; Chrome verified clicking the chevron opens the menu, the menu stays above the asset library, and selecting an option syncs the hidden native select value used by generation.
- [x] AI Agent explicit model routing: chat primary is ModelScope `Qwen/Qwen3-235B-A22B`, chat fallback is Agnes `agnes-2.5-flash`, image plans default to grsai `custom-api / nano-banana-2`, and `runninghub / gpt-4o-mini` is not an Agent default path.
- [x] AI Agent credential guard: Agent chat fallback now requires a configured Agnes key; without ModelScope or Agnes credentials, the API returns an explicit configuration error instead of silently trying an unconfigured platform.
- [x] Smart Canvas AI Agent phase 1: workflow-card UX now renders a visible plan card with route and steps, feedback actions show synced/local/auth-required status in the chat bubble, and authenticated real-model response acceptance is verified through NAS API smoke.
- [x] Production Agent chat credential acceptance: persistent NAS API env now has an Agnes chat key; authenticated `/api/canvas-agent/suggest` returns `agnes-ai / agnes-2.5-flash` for text-only chat and does not use `runninghub / gpt-4o-mini`.
- [x] AI Agent video fallback acceptance: Agnes `agnes-video-v2.0` passed a real NAS `/api/canvas-video` smoke with `provider_id=agnes-ai`, returned `status=200`, one video, and task/video identifiers; it remains optional fallback rather than the main image path.
- [x] Canvas editor layout polish: browser-reviewed classic/smart right-side panels; mobile rules now have regression coverage for side-panel bounds and no horizontal overflow was observed in the Smart Canvas mobile browser check.
- [x] NAS source sync: copy the latest UI polish files to NAS while preserving `.env`, `data/`, and `output/`.
- [x] NAS API Key persistence: persist browser-saved provider keys by mounting `deploy/fnos/api-env` to `/app/API`; keep `.env`, `api-env`, `data`, `output`, and `team-assets` out of source sync/build context.
- [x] Workbench NAS homepage acceptance: NAS source was synced, Docker Compose was rebuilt/restarted, `/healthz` returned ok, and LAN browser checks confirmed the promoted homepage in `frame-workbench` on desktop/mobile without horizontal overflow.
- [x] Workflow Builder v1: fixed built-in workflow templates now appear in the classic/smart workflow asset lists and build canvas nodes/links without calling generation APIs; image workflows now insert an empty image input node when no image is selected.
- [x] Canvas list delete UX: cloud canvas cards expose Delete in the three-dot More menu and delete through the existing Team Cloud canvas endpoint.
- [x] grsai Nano Banana 4K acceptance: `nano-banana-2` and `nano-banana-pro` share the grsai async path for `2K/4K`; preserve `replyType=async`, `imageSize`, numeric task-id polling, and `/api/result` parsing in future refactors.
- [x] Classic canvas secondary tools dock: expanded More tools now avoids clipping/overlap and exposes a visible `??` toggle.
- [x] NAS sync artifact guard: exclude `.venv`, `tmp`, and `.codex-speedtest-current` from NAS sync and Docker build context; cleaned the mistakenly synced copies from NAS.
- [x] Canvas node upload error handling: classic/smart upload-to-node paths no longer directly parse `/api/ai/upload` as JSON; HTML error pages are converted to a clear Chinese upload failure, and >50MB files are blocked before sending.
- [ ] Canvas node upload public browser acceptance: code is pushed and NAS is rebuilt; user verifies the previously failing 4K image on `https://canvas.yiyuqiaoai.uk/` after a hard refresh.
- [ ] Workflow automation phase 2: add optional confirmed auto-run, retry UI, and model fallback selection for generated workflow nodes after separate browser acceptance.

### External Acceptance Gate

| Item | Current Status | Required Environment | Pass Evidence |
| --- | --- | --- | --- |
| Production Agent chat model | Done | Agnes chat API key configured in NAS runtime | Authenticated NAS smoke returned `status=200`, `provider_id=agnes-ai`, `model=agnes-2.5-flash`, `fallback_used=true`, and did not use `runninghub / gpt-4o-mini`. Explicit image planning returned `custom-api / nano-banana-2`. |
| AI Agent video fallback | Done | Agnes video API key configured in NAS runtime | Real LAN `/api/canvas-video` smoke using `provider_id=agnes-ai`, `model=agnes-video-v2.0`, `duration=1`, and safe abstract prompt returned `status=200`, one video, plus task/video identifiers in about 113 seconds. |
| Production Supabase schema | Done | SSH + valid Postgres connection string | `team_cloud_schema.sql` applied successfully; `canvases/assets.visibility` default to `team` and enforce `private/team`. |
| R2 and `TEAM_API_SECRET_KEY` | Done | NAS `deploy/fnos/.env` with real R2 credentials and stable API secret | LAN `/healthz` reports R2/team API readiness; a temporary member generated through the configured grsai `custom-api` provider with `gpt-image-2`, and production generation log recorded `succeeded`. |
| Cloudflare Access non-admin login | Done | Real Chrome Cloudflare Access session | `/api/team-cloud/me` returned `status=200`, `contentType=application/json`, `provider=cloudflare-access`, email `yiyuqiao2748@gmail.com`, default team `8b79d1ce-5782-48ea-bead-a68b5cc56bfb`, and role `member`; typed `yiwei2748@gmail.com` was not the returned account. |
| Team asset API acceptance | Done | Real Supabase access token + R2 | Team asset upload/list/content/delete works through production API and R2. |
| Team asset UI acceptance | Done | Authenticated headless Chrome session with upload permission | Team asset drag-to-canvas saved an image node to the cloud canvas; dragging an output image back into Team Assets created a second team asset. |
| Team API member acceptance | Done | Admin-configured live provider plus regular member account | Temporary member saw API settings hidden, provider management returned 403, and the member generated through grsai `custom-api` / `gpt-image-2` with a succeeded production generation log. |
| Cloud version API acceptance | Done | Real Supabase access token | Stale save returns 409, remote changes are not overwritten, version history lists latest-first, and restore creates a new current version. |
| Cloud version dual-browser UI acceptance | Done | Two independent headless Chrome profiles on the same team cloud canvas | Browser B stale save returned 409 then retried 200; final canvas version was v3 with both browser nodes. Editor history listed v2/v1 and restored v1 to a new v3. |
| Workbench NAS homepage acceptance | Done | NAS mounted source + SSH Docker restart | `docker compose -p yishu-canvas up -d --build` rebuilt and restarted healthy containers; LAN `/` loaded `frame-workbench` with `AI???`, character image, feedback, private canvas, and generation controls on desktop/mobile with no horizontal overflow. |

## 2026-07-27 Frontend Workbench UI Pass

- [x] Added a new `static/workbench.html` home workbench inspired by the provided dark red/gold AI design references.
- [x] Added `static/css/workbench.css` and `static/js/workbench.js` for the standalone home shell.
- [x] Wired `static/index.html` to open the new workbench as the default page.
- [x] Added iframe-to-shell navigation so workbench buttons switch to AI canvas, team, asset manager, and GPT chat without nesting pages.
- [x] Ran JS syntax checks for `static/js/workbench.js` and `static/js/team-cloud.js`.
- [x] Browser screenshot verification completed with headless Chrome for desktop and mobile workbench viewports; mobile composer overflow fixed.
- [x] Next UI slice: connect the prompt composer to the existing canvas creation flow and seed the new canvas with a prompt node instead of only navigating to the canvas workspace.
- [x] Seed the new workbench canvas with an existing generator node and prompt-to-generator connection without auto-running generation.

## 2026-07-27 Privacy Data Model Progress

- [x] Added `visibility` handling for team cloud canvases and team assets: `private` and `team`.
- [x] New team cloud canvases default to `private`; team asset uploads explicitly publish as `team`.
- [x] Added publish endpoints for private canvases and assets.
- [x] Added regression tests for private canvas/asset isolation and publish-to-team behavior.
- [x] Applied updated Supabase schema before production verification: `docs/supabase/team_cloud_schema.sql` was applied and the production schema gate is marked Done above.
- [x] Hardened the schema migration so existing Supabase tables also receive `visibility in ('private', 'team')` constraints.

?????2026-07-27

## ?????

- ???????????????????????????????????????????
- ??????????????????????????????????????????????????
- ?????????????? 0 ????? `docs/migration/local-data-readwrite-audit.md`?
- ????????? `TEAM_ASSET_REQUIRE_R2`????????? R2 ?????????
- AI ????????????????R2 ??????????? `generated/output/*`???? R2 ???????????
- ?????????????????? JPEG ??????????????????
- ?????????????????????????????????????????
- ?? API ???????? API ??????????????????????????????????
- GitHub ??????? UTF-8 GitHub API ??????? Git ???????
- ???????`/healthz` ??? Supabase??? API ??? R2 ????????????????
- GitHub ???????????????? `yiyuqiao2748/yishu-canvas-workbench`?? GitHub ????????
- NAS ????? NAS ??? `Z:\yishu-canvas\yishu-canvas-fnos` ???????????? `.env`?
- Cloudflare Access ??????? Access ?????????? Access JWT ????/????????????????
- ??????????????????????????/???????????
- ???????????????????????? URL ??????? Google ?????????
- ???????????????????????????AI ??? / ???????????????

## ??????

- ?????????????? Cloudflare Access ??????????????????????
- ???????????????????????????????????????
- ??????????????????????????????????????
- API ?????????? API???????? API ?????????????
- ????????????????????/??????????????????/NAS??? NAS ???????????
- NAS ??????????? 115 ??????????? 160G ???????????

## ????/??

- Cloudflare Access ???????? `@qq.com`????? QQ ?????????????????????????????????????????
- NAS ?????? 160G?????????????/?????
- ???????????? GitHub ?????????? NAS ?????????????????????? `.env` ??? GitHub?

## 2026-07-27 ???????

- [x] ????????????`yiyuqiao2748/yishu-canvas-workbench`?
- [x] ????/?????????????? `kind` ???????????????
- [x] ????????????????? URL ????
- [x] ???????????????????????
- [x] ????????AI ??? / ???????????
- [x] ?????????/??????????????
- [x] ???????????????????????
- [x] ?????/???????Cloudflare Access??????????/?????????????? API ????????????

## 2026-07-25 ?? 4 ??

- [x] ?? API ??????????????????????
- [x] ?? API ??????????????????????
- [x] ?? API ??????????????? LLM ?????
- [x] ??????? LLM ??????????????
- [x] ?????????????????
- [x] ?????????????????
- [x] ?? `/healthz` ????????????? `auth_ready`?`supabase_ready`?`team_api_secret_ready`?`r2_ready`?`require_r2`?
- [x] NAS `.env` ?? R2 ??? `TEAM_API_SECRET_KEY` ?? Key ?????`/healthz` ????????????????

## 2026-07-25 ??????????

- [x] ???????????????
- [x] ?????????????????????????????????
- [x] ?????????????????????
- [x] ??????????????????
- [x] ????????????????????
- [x] ????????????????????
- [x] ??????????????????????????????????????
