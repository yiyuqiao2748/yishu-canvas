# T0D0

更新时间：2026-07-25

## 当前已推进

- 智能画布云端模式：已支持轮询云端版本、保存中排队、失败重试、冲突提示和自动合并后重存。
- 普通画布云端模式：已支持自动保存失败重试、云端冲突提示，并在冲突时合并远端新增节点和连线后继续保存。
- 本地数据读写入口：已完成阶段 0 审计，详见 `docs/migration/local-data-readwrite-audit.md`。
- 团队素材存储：新增 `TEAM_ASSET_REQUIRE_R2`，线上可禁止未配置 R2 时静默写入本地盘。
- AI 生成结果存储：新增统一保存入口，R2 配好时生成结果可上传到 `generated/output/*`，未配置 R2 时仍保持本地开发回退。
- 团队素材缩略图：图片素材上传后会生成 JPEG 缩略图，并在素材卡片中优先展示小图。
- 团队素材删除保护：删除素材前会检查云端画布引用，仍被使用时阻止删除并提示相关画布。
- 团队 API 密钥：新增团队级 API 配置后端接口和页面入口，密钥会加密保存，列表只显示已配置状态和尾号。
- GitHub 推送：当前使用 UTF-8 GitHub API 推送，避免本机 Git 登录状态影响。

## 下一步优先级

- 线上验收：团队成员登录、创建团队、创建项目、创建云端画布、上传团队素材。
- 画布验收：分别测试智能画布和普通画布在两个浏览器窗口同时编辑时的保存、提示和合并表现。
- 存储验收：等能操作 NAS 后，配置 Cloudflare R2 并验证团队素材、缩略图、AI 生成结果都返回 R2 地址。
- 阶段 4 继续：让 AI 生成/聊天调用优先读取当前团队的 API 配置，并记录基础调用日志。
- 线上配置：等能操作 NAS 后，将 R2 配齐并把 `TEAM_ASSET_REQUIRE_R2=true`。
- 线上配置：部署阶段 4 前需设置 `TEAM_API_SECRET_KEY`，用于加密团队 API Key。

## 暂时受阻

- 公司电脑当前不能操作飞牛 NAS，所以 NAS `.env`、R2 环境变量和线上容器重启暂时不做。

## 2026-07-25 阶段 4 进展

- [x] 团队 API 配置已接入普通画布和智能画布的图片生成请求。
- [x] 团队 API 配置已接入普通画布和智能画布的视频生成请求。
- [x] 团队 API 配置已接入普通画布和智能画布的 LLM 节点请求。
- [x] 图片生成和画布 LLM 已开始写入基础团队调用日志。
- [x] 团队管理页已新增最近调用日志列表。
- [x] 团队管理页已新增基础用量统计摘要。
- [ ] 下一步做真实线上 Key 验收。

## 2026-07-25 云端画布版本历史进展

- [x] 新增团队云端画布版本列表接口。
- [x] 新增恢复到指定历史版本接口，恢复会生成新的当前版本，不覆盖旧历史。
- [x] 团队管理页云端画布列表已支持查看历史版本。
- [x] 团队管理页已支持从历史版本恢复画布。
- [ ] 下一步做编辑器内版本历史入口和线上双浏览器验收。
