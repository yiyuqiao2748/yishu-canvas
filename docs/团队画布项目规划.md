## 2026-08-05 画布节点上传错误处理修复

- 已完成：普通画布和智能画布的图片/媒体上传到节点流程不再直接把 `/api/ai/upload` 响应按 JSON 解析，避免服务器或登录/代理返回 HTML 时显示 `Unexpected token '<'`。
- 已完成：上传前按后端一致的 50MB 单文件上限做前端拦截，超限时直接显示中文错误；裁剪、宫格切分、节点替换、拖放/粘贴上传和普通画布素材库上传都走统一守卫。
- 验证记录：新增静态回归测试覆盖 HTML 错误页防护和超大文件守卫；`canvas.js`、`smart-canvas.js` 语法检查通过；本地与 NAS LAN 直连接口用 9MB 上传探针均返回 JSON，本地真实 3840x2160 JPEG 上传探针也返回 JSON。
- 已部署：修复已提交并推送到 GitHub `7beeb9c`，NAS 源码已同步到同一提交并完成 Docker 重建；LAN `/healthz`、运行中静态 JS 守卫检查和 9MB 上传接口探针通过。
- 待验收：用户在公网 `https://canvas.yiyuqiaoai.uk/` 强制刷新后，用之前失败的 4K 图片重新上传到节点。

## 2026-08-05 普通画布顶部更多工具栏修复

- 已完成：展开右上角“更多”工具后，工具胶囊不再横向裁切导致按钮残缺；展开态改为更宽的可换行布局。
- 已完成：同一个开关按钮在展开后从“更多”切换为“收起”，并同步切换 lucide 图标，保证用户能找到缩回入口。
- 验收记录：新增静态回归测试覆盖展开态 CSS 和 JS 文案/图标切换；本地全量回归通过，GitHub 已推送主功能提交，NAS 已同步并重建，LAN `/healthz` 与静态 CSS/JS 检查通过。
- 部署修正：NAS 同步脚本和 Docker build context 已排除 `.venv`、`tmp`、`.codex-speedtest-current`，避免本地虚拟环境、截图和测速文件进入部署目录；误同步到 NAS 的临时目录已清理。

## 2026-08-05 Workflow Builder / 云画布删除修复

- 已完成：图生图、图生视频内置工作流拖入普通画布或智能画布时，不再强制要求先选中图片节点；没有选中图片时会创建空图片输入节点，用户可后续拖图/上传补齐。
- 已完成：保留 builder 默认严格校验，只有画布 UI 插入模板时显式开启 `allowEmptyImage`，避免误用底层 API 时静默吞掉缺失图片。
- 已完成：项目画布卡片的三点“更多”菜单在云画布模式下也显示删除操作，并复用现有 Team Cloud `DELETE /canvases/{id}`；本地画布仍保持原来的软删除/回收站流程。
- 验收记录：新增静态回归测试覆盖云画布删除入口和空图片输入工作流插入；JS 语法检查覆盖 builder、普通画布、智能画布和画布列表。

## 2026-08-03 grsai Nano Banana 4K 修复记录

- 已完成：`custom-api / grsai` 的 `nano-banana-2` 与 `nano-banana-pro` 统一走官方 `/v1/api/generate`，不再回退到通用 OpenAI 图片接口。
- 已完成：当用户选择 `2K` / `4K` 时，请求必须保留 `imageSize: "2K" | "4K"`，并发送 `replyType: "async"`；`1K` 可继续使用同步 `json`。
- 已完成：grsai 官方异步任务 ID 可能是 `6-...`、`14-...` 这种数字开头格式，不一定以 `task` 开头；后端必须接受该 ID，并轮询 `/v1/api/result?id=...` 读取 `results[].url`。
- 验收记录：NAS 重建到 `2026.08.03.6` 后，`nano-banana-2` 已能生成 4K 图；`nano-banana-pro` 命中同一 `startswith("nano-banana")` 分支，保留同样 4K async 路径，避免单独改回同步模式浪费积分。
- 回归约束：后续重构 grsai、Agent 生图计划、Workflow Builder 模板或 API provider 路由时，不得删除 `grsai_task_id()`、`replyType=async`、`imageSize` 和 `/api/result` 轮询逻辑。

## 2026-08-03 Workflow Builder v1

- 已完成：普通画布和智能画布共享 `canvas-workflow-builder.js`，固定提供“文生图 / 图生图 / 图生视频”三类内置工作流模板。
- 已完成：模板只构建节点和连线，不自动调用图片或视频生成接口；每次插入带 `operation_id`，失败会回滚本次插入。
- 已完成：图生图和图生视频必须使用当前选中的图片节点；没有选中图片时只提示选择图片，不猜测素材来源。
- 已完成：画布生图模板默认使用 grsai `custom-api / nano-banana-2`，高质量模型保留 `nano-banana-pro`，视频模板使用 Agnes `agnes-video-v2.0` 作为可选兜底节点。
- 已完成：新增中英提示辅助预览接口，支持 translate / optimize 两种模式，保留中文原文，不覆盖原始节点，后续可创建新的英文提示节点。
- 待完成：Workflow Builder phase 2 再做确认后自动运行、失败重试 UI、节点级模型兜底切换和真实浏览器拖拽完整验收。

## 2026-08-02 AI Agent 模型路由补充

- 已完成：Agent 聊天路由显式指向 `modelscope / Qwen/Qwen3-235B-A22B`，不再默认走 `comfly`、`runninghub` 或 `gpt-4o-mini`。
- 已完成：画布生图计划默认指向 grsai `custom-api / nano-banana-2`，高质量选项保留 `nano-banana-pro`；`agnes-image-*` 只作为兜底，不作为主生图模型。
- 已完成：前端 Agent 面板发送最近 10 条对话历史，后端可从 `data/agent/{user_id}/history.json` 兜底恢复上下文，非 JSON 回复按自然语言聊天保留，连续重复回复只重试一次。
- 已完成：聊天兜底增加凭据门禁。只有 Agnes AI 配置了可用 API Key 时才进入 `agnes-ai / agnes-2.5-flash` 兜底；否则返回明确配置错误，不再静默调用未配置平台。
- 已完成：生产 NAS 已配置 Agnes 聊天 Key；真实 `/api/canvas-agent/suggest` 文本聊天返回 `agnes-ai / agnes-2.5-flash`，明确生图请求返回 `custom-api / nano-banana-2` 计划，不走 `runninghub / gpt-4o-mini`。
- 已完成：Agent 工作流回复会在面板内渲染计划卡片，展示操作类型、模型路由和步骤；评分、采纳、拒绝反馈会显示已同步、需登录或本地已记状态。
- 已完成：Agnes `agnes-video-v2.0` 可选视频兜底通过真实 NAS `/api/canvas-video` smoke；安全抽象提示词、`duration=1` 返回 `status=200`、1 个视频和 task/video 标识，耗时约 113 秒。

# 团队专属画布项目规划

更新时间：2026-07-30

## 当前状态

- 原项目备份仓库：`yiyuqiao2748/yishu-canvas`
- 当前团队工作台仓库：`yiyuqiao2748/yishu-canvas-workbench`
- 本地目录：`K:\claudecode\yishu-canvas`
- NAS 部署目录：`Z:\yishu-canvas\yishu-canvas-fnos`，同步时保留 `.env`
- 当前基础版本：已合并原作者 `hero8152/Infinite-Canvas` 最新上游版本 `2026.07.23`
- 当前定位：由本地单机画布，改造成小团队可在线使用的“AI 设计师 / 公司团队工作台”
- 团队规模：少于 10 人
- 预算目标：优先使用免费方案，后续按使用量再升级

## 2026-07-27 阶段进度

- 数据权限：团队云端画布和团队素材已加入 `private` / `team` 可见性模型，新建画布默认私有，发布到团队需要显式操作。
- 接口权限：新增画布发布接口和素材发布接口，普通成员不能通过猜 ID 读取他人的私有画布或私有素材。
- 登录热修：修复团队页读取用户状态时 `escapeAttr is not defined` 导致的登录后状态失败问题。
- 前端工作台：新增 `static/workbench.html` 作为默认首页，视觉方向改为深色红金 AI 设计工作台，保留旧画布、团队、素材库等入口。
- 主入口：`static/index.html` 已接入 `workbench` iframe，并支持工作台按钮切换到 AI 画布、团队、素材库和 GPT 对话。
- 首页替换：已将高保真 `workbench-preview` 视觉升级为正式 `workbench.html` / `workbench.css`，当前正式首页就是参考图方向的版本。
- 首页验收：本机 Playwright 已完成正式首页桌面/移动截图检查，无横向页面溢出和控制台错误；NAS 同步后仍需再验一次线上效果。
- 浏览器标注修改：账号胶囊改为读取当前登录账户，未登录显示“未登录”；“灵感”改为“灵感点”，团队成员显示“无限制”、未登录显示“0”；首页角色图替换为用户自制图片；输入框提示词加入建筑/餐厅方向轮换打字效果；作品/素材卡片预留真实缩略图背景；体验反馈改为提交到后台 JSONL 记录。
- 首页导航重排：主页模式隐藏旧外层侧栏，`workbench.html` 覆盖完整界面；左侧深色胶囊图标栏只保留高频入口，鼠标悬停淡入显示名称，文生图/增强/编辑/角度/在线/GPT 收进次级工具展开层，右上角保留团队、素材、工作流、API 设置入口和黑白切换按钮。
- UI 审查修复：已修复静态资源版本回退、主页 iframe 参数透传、主题状态统一、API 推荐入口白底、角度/增强/GPT 控件审查问题，并新增旧版本静态引用回归测试。
- 验证状态：JS 语法检查通过；Python 全量测试 72 个通过；Edge 审查确认 API/素材 iframe 全屏且旧侧栏隐藏，主页 URL 弹窗聚焦、历史创作携带 `recent=1` 打开最近画布，主页到私有画布可携带 `cloud=1&visibility=private` 参数。

### 下一阶段前端目标

- 继续验收首页真实生成：当前“生成高级设计”已直接调用 `/api/online-image`，下一步用已配置团队默认模型做一次真实出图验收。
- NAS 同步后对正式首页、画布列表、普通画布、智能画布、团队页、素材库做一次真实浏览器截图验收。
- 后续统一画布页视觉风格：进入普通/智能画布后的编辑界面也要向首页工作台风格靠拢，但单独作为下一阶段处理。
- 统一右侧“属性与生成 / 项目与素材”面板布局，让画布编辑页更接近参考图的一体化工作台体验。
- 普通成员入口继续隐藏团队 API 管理，只保留可生成、可用素材、可发布团队内容的必要功能。

### 2026-07-29 UI 审查阶段修复

- 修复 `static/index.html` 等静态页加载旧 `2026.07.26.12` 资源的问题，当前入口恢复到 `2026.07.29/28` 系列缓存版本，并加入测试防回退。
- `workbench.js` 发出的 `studio-open-page` 参数现在会由外层 `index.html` 合并进目标 iframe URL，私有画布入口稳定打开 `canvas-list.html?cloud=1&visibility=private`。
- 主题工具统一使用 `studio_theme` / `canvas_theme`，无参数 `StudioTheme.apply()` 会应用当前保存主题；独立功能页不再因缩放脚本产生横向滚动。
- API 设置页推荐入口、获取 Key 辅助按钮改为深色玻璃态；团队页、画布页、角度页、增强页补齐关键表单和滑杆的可访问名称。
- 角度页的 `/ws/stats` 多标签同步改为显式启用，避免当前 NAS/本地环境缺 WebSocket 支持时产生无关红色控制台噪音。
- 追加浏览器标注修复：`asset-manager` 和 `api-settings` 在外层 `index.html` 中进入沉浸模式全屏展示；素材库增加返回主页按钮；API 推荐页、获取 Key、输入框、提示条继续压成深色玻璃态；首页参考图按钮打开本地文件选择，URL 按钮打开输入弹窗，优化提示词不再跳转，历史创作打开最近画布列表。
- 团队管理/登录页也纳入外层首页沉浸模式，打开 `team-cloud` 时隐藏旧白色侧栏并全屏展示团队账户与管理界面。
- 主题跟随收口：共享主题脚本同时写入 `studio-theme-*` 与 `theme-*` 类名，首页会向 iframe 子页广播主题；画布列表已补齐亮色工作台皮肤，避免从亮色首页进入后仍像另一个深色网站。
- API 入口收口：团队管理页移除独立“团队 API”配置区，后续统一从首页账户/管理员弹窗进入 `api-settings`；未登录会弹出登录窗口，非 owner/admin 会提示无权限。

### 2026-07-30 智能画布 AI Agent 集成计划

- 目标：在 `smart-canvas.html` 内接入一个可收起的 AI Agent 面板，作为智能画布的底部 Composer 扩展，支持自然语言生成提示词、优化选中提示词、推荐参数、创建基础工作流和记录反馈。
- 第一阶段范围：保留现有智能画布引擎，不重写节点系统；新增 `canvas-agent-panel/executor/memory/learning` 前端模块和 `/api/canvas-agent/*` 后端接口。
- 安全边界：Agent 建议接口、反馈、等级和记忆都必须复用团队登录鉴权；不再允许通过前端自带 `x-user-id` 伪造身份；API Key 仍只走后端配置，不进入浏览器存储。
- 计划输出：后端调用现有 `/api/canvas-llm` 能力，但 Agent 层必须清洗 LLM JSON 计划，只允许 `create_prompt`、`optimize_prompt`、`suggest_params`、`create_workflow`、`chat` 五类动作，限制卡片类型、参数范围和单次创建数量。
- 模型路由：Agent 聊天默认优先使用 `modelscope / Qwen/Qwen3-235B-A22B`，不可用时才走 `agnes-ai / agnes-2.5-flash`；不再默认依赖未验证的 `runninghub / gpt-4o-mini` 链路。
- 生图边界：画布生图计划默认指向 grsai `custom-api / nano-banana-2`，高质量选项保留 `nano-banana-pro`；`agnes-image-*` 只作为兜底平台，不作为画布主生图模型。
- 执行策略：默认只创建/修改画布节点和参数，不自动触发高成本真实生成；后续需要把“自动生成图片/视频”放到二次确认动作里。
- 记忆策略：本阶段的“自我提升”定义为偏好、评分、成功率、常用风格和能力等级统计，不宣称模型训练；本地 `localStorage` 做即时体验，后端 `data/agent/{user_id}` 做登录用户持久化。
- 当前已完成的第一步：前端 Agent 面板改为统一调用 `/api/canvas-agent/suggest`，执行器写入智能画布真实提示词字段 `node.text`，服务端对 LLM JSON 计划做白名单清洗，浏览器冒烟确认未登录返回 401、规范化 `prompt/image/loop` 卡片能创建提示词节点、选中提示词可被优化、参数建议只应用已确认字段，登录 token 存在时可回填后端记忆/等级，未登录打开面板不会被动打 401，并补充静态回归测试防止绕过 Agent API 或写错节点字段。
- 当前已完成的模型路由修复：`/api/canvas-agent/suggest` 接收最近 10 条对话历史，后端可从 `data/agent/{user_id}/history.json` 兜底恢复上下文；非 JSON 模型回复按自然语言聊天展示；最近三条助手回复完全重复时只重试一次并要求换角度回答；返回 plan 会带 `provider_id`、`model`、`fallback_used`。
- UI 审查补齐：智能画布右上角工具组已加入“主页”返回入口，并统一为红黑玻璃态；AI Agent 面板改为画布底部居中悬浮 Composer，不再横向铺满到底，颜色与当前工作台皮肤一致。
- 普通画布补齐：`canvas.html` 已加载共享 Agent 面板，并通过 `canvas-agent-classic-bridge.js` 只调用现有公开节点创建函数，不重写普通画布节点状态、连线或执行逻辑；普通画布的画布底色、网格、顶部工具组、左侧工具栏、缩略图、LLM/API 节点输入输出、工作流/资产库/日志等浮层已扫掉旧蓝灰面板，视觉与智能画布靠齐。
- 当前验收：登录后真实 Agent 建议、工作流计划卡片、反馈同步显示和后端记忆回填已完成；“生成一张海报”返回 Nano Banana 计划卡片且不自动出图；Agnes `agnes-video-v2.0` 已通过真实接口 smoke，但仍定位为可选视频兜底，不替代主生图链路。

## 总目标

把现有画布改造成团队内部在线系统，让成员可以通过网页访问，不再需要下载安装到本地。系统需要支持账户注册、团队子账号、项目共享、团队素材库、云端保存画布，以及后续自动部署到 GitHub 关联的线上环境。

## 推荐免费架构

| 模块 | 推荐服务 | 用途 |
| --- | --- | --- |
| 代码托管 | GitHub | 存放代码、版本管理、协作开发 |
| 自动部署 | GitHub Actions | 推送代码后自动发布 |
| 后端服务 | Render Free | 部署现有 FastAPI 后端 |
| 用户认证 | Supabase Auth | 注册、登录、团队成员身份 |
| 数据库 | Supabase Postgres | 保存用户、团队、项目、画布、权限 |
| 文件存储 | Cloudflare R2 Free | 保存图片、视频、素材、缩略图 |
| 静态资源 | 后端静态托管或 Cloudflare Pages | 承载现有 `static/` 前端页面 |

当前实机方案：

| 模块 | 当前选择 | 状态 |
| --- | --- | --- |
| 代码托管 | GitHub 新仓库 `yishu-canvas-workbench` | 进行中 |
| 运行环境 | 飞牛 NAS Docker Compose | 已跑通 |
| 外网访问 | Cloudflare Tunnel `https://canvas.yiyuqiaoai.uk` | 已跑通 |
| 外层鉴权 | Cloudflare Access | 已接入 |
| 应用用户 | Cloudflare Access JWT 自动映射到 Supabase 团队用户 | 已实现，待真实同事邮箱验收 |
| 数据库 | Supabase Postgres | 已接入 |
| 团队素材 | Cloudflare R2 + NAS 本地回退 | 已接入，R2 图片显示和代理已修复过一轮 |
| 生成结果 | 默认优先留在用户当前画布/本机浏览器，需要共享时再上传团队素材 | 目标策略 |

说明：GitHub Pages 只能托管静态页面，不能单独运行 FastAPI、数据库、登录系统和文件上传，所以不能只靠 GitHub Pages 完成团队在线版。

## 核心功能规划

### 1. 账户系统

- 用户注册
- 用户登录
- 用户退出
- 登录状态保持
- 密码重置
- 邮箱验证
- 个人资料

### 2. 团队管理

- 创建团队
- 邀请成员
- 成员加入团队
- 成员角色管理
- 移除成员
- 团队基础设置

建议角色：

| 角色 | 权限 |
| --- | --- |
| Owner | 管理团队、成员、密钥、所有项目 |
| Admin | 管理成员和团队项目 |
| Member | 创建、查看、编辑授权项目 |

### 3. 项目共享

- 创建项目
- 项目列表
- 项目归属团队
- 项目成员权限
- 团队内共享项目
- 项目归档
- 项目删除和恢复

### 4. 云端画布

- 画布保存到数据库
- 画布自动保存
- 画布版本记录
- 画布恢复历史版本
- 团队成员可打开同一个项目画布
- 防止多人覆盖保存

### 5. 团队素材库

- 上传图片
- 上传视频
- 保存 AI 生成结果
- 素材归属团队
- 素材归属项目
- 素材分类
- 缩略图缓存
- 删除素材时检查引用关系

### 6. API 密钥管理

- 团队级 API Key 配置
- 后端加密保存密钥
- 普通成员不可直接查看密钥
- 记录模型调用日志
- 记录调用人、项目、模型、时间、状态
- 后续可扩展成本统计

### 7. 在线部署

- Docker 化 FastAPI 服务
- 配置生产环境变量
- GitHub Actions 自动部署
- Render 绑定 GitHub 仓库
- 配置 Supabase
- 配置 Cloudflare R2
- 绑定团队访问域名

### 8. 公司团队工作台体验

- 普通成员进入后直接到画布/工作台，不需要配置 API
- 管理员可配置团队 API、模型和密钥
- 团队 API Key 只保存在后端/数据库加密字段，前端不回显完整 Key
- 普通成员使用团队默认模型生成，不互相看到个人未共享画布
- 生成图片优先保存在自己的当前画布，需要共享时再保存到团队素材
- 团队素材作为共享资产库，才写入 R2/NAS
- 普通画布和智能画布的右侧资产库布局保持一致
- 整体视觉改为深色红金“AI 设计师 / 公司团队工作台”风格

## 数据改造规划

现项目大量数据保存在本地文件中，团队在线版需要逐步迁移到数据库和对象存储。

### 需要迁移的数据

| 当前位置 | 目标位置 |
| --- | --- |
| `data/canvases/*.json` | Supabase Postgres |
| `data/conversations/` | Supabase Postgres |
| `data/asset_library.json` | Supabase Postgres |
| `assets/input/` | Cloudflare R2 |
| `assets/output/` | Cloudflare R2 |
| `assets/uploads/` | Cloudflare R2 |
| `data/media_previews/` | Cloudflare R2 或后端缓存 |
| `API/.env` | Render 环境变量 + 数据库加密配置 |

### 建议数据表

- `users`
- `teams`
- `team_members`
- `invitations`
- `projects`
- `canvases`
- `canvas_versions`
- `assets`
- `api_providers`
- `generation_logs`
- `audit_logs`

## 阶段路线图

### 阶段 0：基线整理

状态：已完成

- [x] 下载项目到 `D:\codex code\yishu-canvas`
- [x] 合并原作者最新版本
- [x] 推送更新到 GitHub
- [x] 跑通现有测试
- [x] 保存团队版项目规划
- [x] 建立开发分支
- [x] 梳理当前本地数据读写入口
- [x] 标记必须迁移的文件存储逻辑

### 阶段 1：认证和团队基础

状态：进行中

- [x] 接入 Supabase Auth 后端校验骨架
- [x] 新增 Supabase 团队表结构脚本
- [x] 新增团队创建、成员列表、成员邀请 API 骨架
- [x] 新增本地开发存储，方便没有 Supabase 配置时先跑通流程
- [x] 新增团队基础测试
- [x] 新增登录页
- [x] 新增注册页
- [x] 新增退出逻辑
- [x] 后端校验登录用户
- [x] 新增团队表
- [x] 新增团队成员表
- [x] 新增团队邀请流程后端骨架
- [x] 前端显示当前团队和当前用户

### 阶段 2：项目和画布云端化

状态：进行中

- [x] 新增项目表
- [x] 新增项目后端 API
- [x] 新建项目保存到本地开发存储或 Supabase
- [x] 新增云端画布后端 API
- [x] 画布保存到本地开发存储或 Supabase
- [x] 增加画布版本记录
- [x] 增加项目权限校验
- [x] 团队页新增项目和云端画布列表
- [x] 项目列表接入原无限画布入口
- [x] `canvas-list.html` 增加团队云端模式
- [x] 团队云端模式下可创建项目和云端画布
- [x] 云端画布打开编辑器
- [x] 智能画布编辑器支持团队云端读取和保存
- [x] 普通画布编辑器支持团队云端读取和保存
- [x] 云端画布保存冲突返回当前画布数据
- [x] 智能画布团队云端模式支持轮询云端版本并合并最新画布
- [x] 智能画布自动保存支持保存中排队、失败重试和云端冲突提示
- [x] 普通画布自动保存支持失败重试、冲突提示和远端新增节点合并
- [ ] 画布自动保存体验打磨
- [ ] 云端画布多人合并策略打磨

### 阶段 3：素材库云存储

状态：代码完成，待 R2/NAS 线上验收

- [x] 接入 Cloudflare R2/S3 兼容存储适配
- [x] 抽象文件存储接口
- [x] 新增团队素材上传 API
- [x] 新增团队素材列表 API
- [x] 上传素材保存到 R2 或本地开发存储
- [x] 素材元数据保存到数据库或本地开发存储
- [x] 团队素材库前端接入
- [x] 新增 `TEAM_ASSET_REQUIRE_R2`，线上可禁止团队素材静默落本地盘
- [x] 新增 AI 生成结果统一文件存储入口，R2 配好时可返回 R2 公网 URL
- [x] 图片团队素材上传后生成缩略图并在前端优先展示
- [x] AI 生成结果保存到 R2 的代码入口
- [x] 缩略图生成和缓存
- [x] 删除素材时检查画布引用

### 阶段 4：团队 API 密钥

状态：进行中

- [x] API Key 加密存储
- [x] 团队级 API 配置页
- [x] 普通成员隐藏密钥明文
- [x] 模型调用统一走后端
- [x] 记录调用日志
- [x] 增加基础用量统计
- [x] 管理员可配置团队 API，普通成员使用但不看 Key
- [ ] 将团队 API 设置页完全搬迁为现有 API 设置同款交互：平台列表、验证地址、验证协议、手动添加模型、选择模型
- [ ] 普通成员默认隐藏 API 设置入口

### 阶段 5：线上部署

状态：进行中

- [x] 新增 Dockerfile
- [x] 新增生产环境配置
- [x] 新增 Render Blueprint
- [x] 新增 GitHub Actions CI
- [x] 新增健康检查接口
- [x] 新增飞牛 NAS + Cloudflare Tunnel 部署模板
- [x] 飞牛 NAS Docker 实机部署跑通
- [x] Cloudflare Tunnel 跑通 `https://canvas.yiyuqiaoai.uk`
- [x] Cloudflare Access 已接入外层访问保护
- [x] Cloudflare Access JWT 自动登录/自动加入默认团队逻辑已实现
- [x] Supabase 项目创建完成
- [x] Supabase 数据库表结构执行成功
- [x] NAS `.env` 已接入 Supabase，`auth_ready=true`、`supabase_ready=true`、`dev_bypass=false`
- [x] 配置 R2 环境变量
- [ ] 团队成员登录测试
- [ ] 验证 Cloudflare Access 策略是否只放行可信邮箱范围
- [ ] 公司电脑远程续做流程：GitHub 拉代码、NAS 共享盘同步、Docker 重建

### 阶段 6：协作体验增强

状态：进行中

- [ ] 多人在线状态
- [x] 画布保存冲突提示
- [x] 画布历史版本恢复
- [ ] 团队素材库筛选
- [x] 项目/画布删除和恢复
- [ ] 项目归档
- [ ] 管理后台

### 阶段 7：视觉和资产体验

状态：进行中

- [x] 修复云端画布类型串味：普通/智能画布按各自 `kind` 打开
- [x] 修复素材拖入画布触发浏览器新标签页或 Google 搜索
- [x] 普通画布、智能画布、项目列表页统一深色红金工作台风格
- [x] 主入口侧栏隐藏项目主页和手动更新入口，仅保留版本号
- [x] 主入口“无限画布”默认文案改为“画布”
- [ ] 真实浏览器验收参考图风格：顶栏、右侧生成面板、项目与素材面板
- [ ] 团队素材、本地素材、画布节点之间做最终拖拽体验打磨
- [ ] 生成图本地优先、手动共享到团队素材

### 阶段 8：NAS 存储维护

状态：待实施

- [ ] 统计 NAS 写入/读取和公网访问速度
- [ ] 团队共享素材保留在 NAS/R2
- [ ] 个人临时生成结果默认不长期写入 NAS
- [ ] 增加 30 天清理策略
- [ ] 清理前转存到 115 远程挂载作为归档备份
- [ ] 在文档中记录恢复流程

## 优先级建议

第一优先级：

- 登录注册
- 团队成员
- 项目共享
- 画布云端保存

第二优先级：

- 素材云存储
- API Key 团队管理
- 部署上线

第三优先级：

- 多人实时协作
- 版本恢复
- 用量统计
- 管理后台

## 免费方案风险

- Render 免费服务空闲后会休眠，首次访问会慢。
- Supabase 免费数据库空间有限，适合先存账户、项目和画布，不适合直接存大量图片。
- Cloudflare R2 免费额度够小团队起步，但视频生成多了会较快占用空间。
- AI 模型调用费用不属于托管免费额度，需要由团队自己的 API Key 承担。

## 当前下一步

当前团队云 MVP 的账户、团队、项目、云端画布、素材上传和团队素材前端入口已经合并到 `main` 并推送到 GitHub。飞牛 NAS Docker、Cloudflare Tunnel、Cloudflare Access、Supabase 项目和数据库表结构已经跑通，NAS 环境已进入真实 Supabase 鉴权模式。

建议下一步在公司电脑先 `git pull` 同步最新代码，然后做线上验收收口：测试团队成员登录、创建团队/项目/云端画布、上传团队素材，并确认智能画布和普通画布的云端同步、自动保存重试提示。等能操作 NAS 后配置 R2，验证团队素材、缩略图和 AI 生成结果都返回 R2 地址；阶段 4 继续把 AI 生成/聊天调用切到当前团队 API 配置，并补基础调用日志。
# 2026-07-25 阶段 4 更新

- 团队 API Key 已支持加密保存，只有 Owner/Admin 可以维护，普通成员只能看到已配置状态和尾号。
- 普通画布、智能画布的图片生成、视频生成、画布 LLM 请求已携带团队/项目/画布上下文，后端会优先读取当前团队 API 配置。
- 图片生成和画布 LLM 已开始写入基础团队调用日志，团队管理页已能查看最近调用记录和基础用量统计。
- 仍需在能操作 NAS 后设置稳定的 `TEAM_API_SECRET_KEY`，并用真实团队 Key 做线上验收。

# 2026-07-25 云端画布版本历史更新

- 后端已新增云端画布版本列表接口。
- 后端已新增恢复指定历史版本接口，恢复会生成新的当前版本，不覆盖旧版本记录。
- 团队管理页已能查看某个云端画布的历史版本，并从历史版本恢复。
- 普通画布/智能画布编辑器内部已新增版本历史入口，可查看历史版本并恢复；后续继续做线上双浏览器验收。

## 2026-08-25 Public performance and Smart Image Agent refinement

- [x] Replace the canvas-list N+2 request chain with one authenticated workspace summary and user/team-scoped stale-while-refresh browser cache.
- [x] Reduce login duplication with cached username resolution, stage timing, immediate login-response state, and one background summary refresh.
- [x] Load Smart Canvas data before assets/config, remove the duplicate final render, and keep decoded original previews in a bounded page cache.
- [x] Add explicit provider reference-image modes; `custom-api-2` uses `generations_image_urls` after persistent production configuration.
- [x] Persist Agent `1K/2K/4K` resolution and support explicit cumulative canvas-image references up to 10 items.
- [x] Apply the production migration and persist the backed-up `custom-api-2` reference protocol without changing API keys or runtime assets.
- [ ] Deploy version `2026.08.25.2` and complete real-account browser acceptance.
- [ ] Add the scoped Cloudflare HTML Cache Rule and verify a second request reports `CF-Cache-Status: HIT`; the NAS Tunnel token cannot manage Zone rules.
- [ ] Split the monolithic Smart Canvas bundle into independently lazy-loaded editor, log, asset, and low-frequency generation chunks after this lower-risk release is stable.

# 2026-07-28 Canvas Page Visual Alignment

- Canvas list, classic canvas, and smart canvas now have a first-pass workbench visual skin: dark grid background, floating capsule docks, compact tools, dark side panels, and red/orange primary actions.
- Canvas mode now hides the old white outer sidebar; classic canvas keeps high-frequency node creation on the left and moves low-frequency tools/history/workflow/assets/log controls to the top-right capsule group.
- This pass only changes the visual layer and CSS cache versions; it does not change node, drag, save, or generation behavior.
- Next polish pass should refine the right-side "properties/generation" and "project/assets" panel density after browser review.

# 2026-07-28 Secondary Page Browser Annotation Polish

- Fixed remaining style mismatches reported from the browser: team cloud, API settings, enhance, Klein image edit, angle control, and GPT chat now use dark red/black glass surfaces instead of white fallback panels.
- Classic canvas secondary actions now collapse by default behind an arrow control, reducing topbar obstruction while preserving the existing tool buttons.
- Canvas LLM/API prompt and output blocks now use dark surfaces in dark mode, including generated prompt text blocks.
- Canvas list now includes a clear home/workbench return button.
- Workbench quick-rail history now routes to the canvas list instead of the team page, and the standalone black/white toggle applies the local theme without requiring the outer shell.
- Verification completed locally with JS syntax checks, Python tests, and browser computed-style checks; NAS deployment still requires container rebuild/restart after sync.
- NAS source files have been synced to `\\100.66.1.1\docker\yishu-canvas\yishu-canvas-fnos` and key file hashes match local; `.env`, `data/`, and `output/` were not copied. Runtime acceptance remains pending until Docker is rebuilt/restarted.

# 2026-08-12 Public Loading Performance

- [x] Added repeatable LAN/public timing reports, request IDs, `Server-Timing`, and browser performance marks.
- [x] Reduced homepage startup to one deferred workbench summary request and reused the Supabase Auth HTTP connection pool.
- [x] Added allowlisted 256/512/1024 WebP previews, media dimensions, two-worker historical backfill, and original-image-on-demand behavior.
- [x] Added viewport-bounded classic/smart rendering with `?renderer=legacy` rollback; local 100-node smoke rendered 20-24 DOM nodes instead of 100.
- [x] Replaced Tailwind CDN runtime and full Lucide bundle with precompiled Tailwind and a scanned icon subset.
- [x] Split the large homepage inline CSS/JS, minified homepage/workbench/canvas scripts, delayed Agent modules until first use, and enabled gzip.
- [x] Added explicit HTML/dist/private API cache boundaries, guarded NAS sync exclusions, and deployment prewarm tooling.
- [x] Completed local rollout verification: 141 Python tests, 10 key JS syntax checks, and classic/smart 100-node browser pressure checks passed; viewport rendering kept 20/24 nodes mounted while `?renderer=legacy` kept all 100.
- [x] Production deployment: protected NAS sync and no-cache Docker rebuild completed; `/healthz` is healthy, request timing is present, persistent env hashes are unchanged, and repeated static assets reach `CF-Cache-Status: HIT`.
- [x] GitHub publication: remote `main` was advanced through the Git data API after the ordinary HTTPS pack route repeatedly reset; the published final tree matches local rollout commit `a89687c` exactly.
- [ ] Production final gates: add the Cloudflare 5-minute HTML Cache Rule (`/` and static HTML remain `DYNAMIC`), verify a real-account login P95, and capture authenticated public canvas first-node/preview metrics on a stable browser route.
## 2026-08-24 智能画布 Lovart 式图片创作路径

- [x] 仅智能画布的图片 Agent v2 增加 `New chat / History` 会话入口；会话绑定用户和画布，支持自动标题、恢复和归档，普通画布不加载该模块。
- [x] 图片引用显示并允许调整 `主图 / 参考 / 待修改` 角色；方案只使用已验证的图片 URL，单会话只保留一个待确认方案以避免重复扣点。
- [x] 图片技能收敛为海报、商品主图、社媒套图、风格变体、扩图补景和多图合成；均复用既有 Nano Banana 执行链路，不增加视频、RunningHub 或 Agnes 生图兜底。
- [x] 结果支持继续修改、变体、扩图、定位、下载和保存素材；当前结果组可处理“第 N 张”后续指令。
- [x] Smart Image Agent 增加紧凑的五项 Smart Canvas 控制条：适应全部、放大、缩小、重置缩放和整理选中节点。Bridge 仅暴露这些方法，非法图片路由提示覆盖四个已配置图片模型；本地静态/单元测试、JavaScript 检查、Python 编译和静态脚本构建均已通过。
- [ ] 生产与真实账号验收仍待完成：在已部署的 Smart Canvas 会话中验证五项控制，并继续完成单图、选图修改和四图批量生成验收。
- [ ] 生产验收：部署后由真实账号完成单图、选图修改和四图批量生成，核对积分、任务阶段、素材、画布保存与刷新恢复。
